# Copy to .env and fill in. Never commit the real .env file.

# Anthropic API key (added when the agent loop is wired in Phase 4)
ANTHROPIC_API_KEY=

# CORS origin for the deployed/local frontend (added in Phase 6)
FRONTEND_ORIGIN=http://localhost:3000
